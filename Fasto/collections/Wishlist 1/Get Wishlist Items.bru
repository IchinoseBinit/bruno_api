meta {
  name: Get Wishlist Items
  type: http
  seq: 2
}

get {
  url: {{gateway}}/wishlist/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
