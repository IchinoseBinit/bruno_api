meta {
  name: Get Wishlist Items copy
  type: http
  seq: 5
}

get {
  url: {{gateway}}/wishlist/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
