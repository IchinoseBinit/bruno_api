info:
  name: 2. Get job status
  type: http
  seq: 3

http:
  method: POST
  url: "{{gateway}}/api/product/image-enhancer/2/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "prompt": ""
      }
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Poll this every ~2s until status is 'done' or 'failed'. When done, result_image is the URL of the generated image.
