info:
  name: Get Payment Modes
  type: http
  seq: 1

http:
  method: GET
  url: "{{gateway}}/payment/modes/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List active payment modes. After running migration 0014, expect a row with `type='fonepay'` in the response.
