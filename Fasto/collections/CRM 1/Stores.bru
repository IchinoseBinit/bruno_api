meta {
  name: Stores
  type: http
  seq: 16
}

get {
  url: {{gateway}}/stores
  body: none
  auth: bearer
}

auth:bearer {
  token: {{packerAccessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
