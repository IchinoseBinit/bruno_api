meta {
  name: Support Configuration
  type: http
  seq: 32
}

get {
  url: {{gateway}}/system/support-config
  body: none
  auth: bearer
}

auth:bearer {
  token: {{packerAccessToken}}
}

settings {
  encodeUrl: true
}
