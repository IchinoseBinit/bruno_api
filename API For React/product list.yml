info:
  name: product list
  type: http
  seq: 3

http:
  method: GET
  url: "{{gateway}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
